syntax = "proto3";

package proto.v1;

option go_package = "github.com/sis-shen/sup-iam/internal/proto/tpc/v1;pbv1";

// Secret 密钥信息
message Secret {
  string secret_id = 1;
  string secret_key = 2;  // 用于签名校验的 Secret Key
  string access_key = 3;  // 用于检验rpc查询的access_key
  int64  expires_at = 4;  // Unix 时间戳，0 表示永不过期

}

message Policy {
  string policy_id = 1;
  string username = 2;
  string policy_dsl = 3;
}

